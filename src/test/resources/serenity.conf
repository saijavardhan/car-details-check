serenity.project.name="Car Details Check"
serenity.requirement.types= "epic, feature"
serenity.restart.browser.for.each = "scenario"

webdriver {
    driver=chrome
}

environments {
    default {
        webdriver.base.url = "https://cartaxcheck.co.uk"
    }
}

webdriver.autodownload=true